# 任务详情页红框复刻任务书

## 目标

在现有 Vue Demo 基础上，将 `/tasks/detail` 重构为参考图所示的“任务刚创建、草稿、尚未添加资料”详情页。页面必须在 1600×1000 基准视口高度还原，并在 1366×768、1440×900、1920×1080 下按比例协调、自适应、不出现横向溢出或大面积无意义空白。

## 唯一视觉基准

- 参考图：`C:\Users\ZHUANZ~1\AppData\Local\Temp\codex-clipboard-4f58d7ed-59a1-4154-9703-280666294368.png`
- 基准尺寸：1600×1000
- 用户红框定义功能边界，红框本身不属于最终界面。
- 原始设计说明定义业务状态：任务已创建、资料为空、仅创建任务完成，其余流程未开始。

## 成功标准

- [x] 页面结构与参考图逐区一致：应用头部、左导航、任务头、详情元数据、标签页、空状态与三入口、能力卡、右侧提示、任务流程、操作留痕。
- [x] 文案、状态、数量、动作顺序和视觉层级与参考图一致。
- [x] 1600×1000 首屏内完整展示底部流程和右下操作留痕，无额外大空白。
- [x] 1366×768 保持可用：内容可纵向滚动，关键操作不被裁切，横向不溢出。
- [x] 1440×900、1920×1080 比例协调；超宽时内容区受控，不盲目拉伸卡片。
- [x] 所有按钮仍为语义化按钮/链接，键盘可达；页面无控制台错误。
- [x] 只运行与本次变动相关的测试、构建和多视口视觉检查，不运行无关全量测试。

## 假设与边界

- 本次只重做任务详情页及其必要的局部组件/局部样式；不重构其他业务页。
- 允许为了匹配参考图给任务详情页增加专属布局变体，但不应破坏全局其他路由。
- 参考图优先于现有任务详情页的“处理中”业务内容；本页默认呈现刚创建草稿态。
- 工作区持续存在大量并行修改；本任务只精确提交自身文件，不清理、不重置其他修改。

## 红框区域拆分

1. 全局应用壳：品牌区、顶部导航、工具区、左侧主导航与收起入口。
2. 任务头：返回、标题、草稿标签、7 项任务元数据、3 个动作按钮。
3. 详情标签栏：11 个标签，任务概览激活。
4. 主工作区左上：空资料状态插画、标题说明、3 个资料入口。
5. 主工作区左中：所选审计能力与 3 张能力卡。
6. 主工作区左下：7 步任务流程时间线。
7. 主工作区右上：下一步提示，含待补充资料、所需输出、阻断项。
8. 主工作区右下：操作留痕表格、查看日志、分页。

## 多 agent 分工与提示词

### Agent A：视觉测量

> 你是视觉测量 agent。只读，不修改任何文件。工作区：C:\Users\ZhuanZ（无密码）\Desktop\东方证券demo\audit_lifecycle_new_demo。目标参考图：C:\Users\ZHUANZ~1\AppData\Local\Temp\codex-clipboard-4f58d7ed-59a1-4154-9703-280666294368.png（1600×1000，红框是用户划分的功能区域）。当前旧页面截图：C:\Users\ZhuanZ（无密码）\Desktop\东方证券demo\audit_lifecycle_new_demo\docs\10-final-acceptance\screenshots\tasks__detail.png。任务：1) 按红框逐区编号拆解所有可见元素；2) 估算每区 x/y/宽/高、内外边距、栅格比例、字体层级、颜色/边框/圆角；3) 说明 1600×1000 原图中主内容宽度、左栏/右栏/顶部高度、纵向节奏；4) 对比旧截图列出至少 15 个高影响差异；5) 给出 1366×768、1440×900、1920×1080 的自适应规则，重点避免大面积空白，允许整体同比缩放但不能破坏信息层级。输出：结构化中文报告，含区域表、差异优先级 P0/P1/P2、验收尺寸建议。不要写代码，不要创建文件。

### Agent B：代码审计

> 你是代码审计 agent。只读，不修改任何文件。工作区：C:\Users\ZhuanZ（无密码）\Desktop\东方证券demo\audit_lifecycle_new_demo\app。目标是把 /tasks/detail 从现状改成用户参考图中的“任务刚创建、尚未添加资料”详情页。请审计：src/views/tasks/TaskDetail.vue、src/components/layout/AppLayout.vue、src/styles/*.css、src/mock/index.js、src/router/index.js，以及相关测试/脚本。任务：1) 画出当前组件/数据/样式依赖；2) 指出造成当前页面与目标差距的结构性原因；3) 明确可复用与应重写的部分；4) 给出最小且清晰的文件改动清单；5) 识别潜在副作用（其他路由共用全局 CSS/布局）；6) 建议只针对本次改动的测试命令和应新增的精确测试。输出：中文结构化报告，包含具体文件和关键行号、推荐组件边界、不要触碰的内容。不要写代码，不要创建文件。

### Agent C：运行基线

> 你是运行基线 agent。只读源代码，不修改或创建项目文件。工作区：C:\Users\ZhuanZ（无密码）\Desktop\东方证券demo\audit_lifecycle_new_demo\app。请检查依赖是否已安装；必要时启动 npm run dev（使用本机 localhost，记录端口），访问 /tasks/detail。任务：1) 在现有页面记录 1600×1000、1440×900、1366×768、1920×1080 的首屏布局（可截图到系统临时目录，不要写入项目）；2) 检查横向溢出、纵向大空白、首屏内容密度、右栏掉落、侧栏/顶栏尺寸；3) 记录浏览器控制台错误/警告；4) 找出运行时实际路由状态和数据状态为何不是“刚创建草稿”；5) 给出后续视觉验收步骤与可自动化的 DOM 尺寸断言。输出中文证据报告，列出命令、URL、关键像素/现象；不要修改项目文件。若无法启浏览器，说明明确阻碍并继续静态检查。

## 执行阶段

- [x] 阶段 0：建立任务书与成功标准。
- [x] 阶段 1：只读探索项目、参考图、当前截图与运行基线。
- [x] 阶段 2：汇总多 agent 证据，形成详细设计与 2–3 种实现路线，交用户确认。
- [x] 阶段 3：写入设计说明并自审；用户确认书面设计。
- [x] 阶段 4：生成细粒度实施计划和局部测试计划。
- [x] 阶段 5：实现应用壳与任务头。
- [x] 阶段 6：实现标签、空状态和资料入口。
- [x] 阶段 7：实现能力卡和右侧提示。
- [x] 阶段 8：实现时间线和操作留痕。
- [x] 阶段 9：相关测试、构建和 4 视口视觉复核。
- [x] 阶段 10：再次对照原图，修正残余差异并完成验收。

## 防遗忘检查点

- 每个阶段开始前重读本文件的“目标 / 成功标准 / 当前阶段”。
- 每次代码改动后只运行直接相关测试；视觉变动后重截对应视口。
- 每轮视觉修正记录在 `notes.md`，不靠聊天上下文保存。
- 不以“页面能打开”作为完成；必须有原图对照、多视口截图和 DOM/控制台证据。

## 问题与阻碍日志

- 2026-07-10：仓库 `main` 尚无提交，现有内容大多为已暂存文件，不能依赖 git 历史回退；必须保持手术式编辑并逐次查看 diff。
- 2026-07-10：现页 1600×1000 下文档高约 1578px，详情主体从 y≈744px 才开始；1366×768 首屏仅露出约 24px 主体。根因是旧结构堆叠，而非单一间距问题。
- 2026-07-10：方案 A 与书面设计已获用户确认，并以 `87533c1` 独立提交；随后按 7 个任务实施。
- 2026-07-10：用户已确认书面设计。实施计划 `docs/superpowers/plans/2026-07-10-task-detail-draft-redline-rebuild.md` 自审通过，共 7 个任务、41 个步骤。
- 2026-07-10：相关基线 `taskDetail*.test.js` 为 38 通过、1 个既有失败；失败来自并行生成中/归档模板顺序与归档静态测试不一致，不属于草稿态改动，本轮不修改该测试。
